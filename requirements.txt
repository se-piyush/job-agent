# Core orchestration (anthropic extra required for native Claude provider)
crewai[anthropic]>=0.80.0
crewai-tools>=0.14.0

# MCP (linkedin-mcp-server transport)
mcp>=1.0.0

# PDF generation
playwright>=1.40.0

# Utilities
python-dotenv>=1.0.0
click>=8.1.0
