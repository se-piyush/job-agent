# Core orchestration
# [anthropic] extra required for the native Claude provider used by the resume agent.
# litellm is installed separately (not via crewai extra) to avoid version conflicts
# with crewai-tools which pins crewai to an exact version.
crewai[anthropic]>=0.80.0
# LiteLLM enables Groq, Together AI, Fireworks, OpenRouter and all other
# non-native providers (anything not in crewai's built-in list).
litellm>=1.0.0
crewai-tools>=0.14.0

# MCP (linkedin-mcp-server transport)
mcp>=1.0.0

# PDF generation
playwright>=1.40.0

# Utilities
python-dotenv>=1.0.0
click>=8.1.0
